@model LabaOne.Country

@{
    ViewData["Title"] = "Деталі";
}

<h1>Деталі</h1>

<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.CountryName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.CountryName)
        </dd>
    </dl>
        <h6>@Html.DisplayNameFor(model => model.Variants)</h6>
    <dl>
        @foreach(var c in Model.Variants)
        {
            <dd class = "col-sm-10">
            @Html.DisplayFor(model => c.VariantName)</dd>
        }
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id">Деталі</a> |
    <a asp-action="Index">Назад до списку</a>
</div>
